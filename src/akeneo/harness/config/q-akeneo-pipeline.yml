
command('app build'): |
  #!bash(workspace:/)|@
  passthru docker-compose build --pull console
  passthru docker-compose build --pull php-fpm nginx
  passthru docker-compose build --pull job-queue-consumer cron
