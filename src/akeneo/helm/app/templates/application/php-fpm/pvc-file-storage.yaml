apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: php-fpm-pv-claim-storage
  labels:
    app.service: php-fpm
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
