  requests:
    memory:
      console: {{ @('resources.limits.memory.console') }}
      cron: {{ @('resources.limits.memory.cron') }}
      php_fpm: {{ @('resources.limits.memory.php_fpm') }}
      app_init: {{ @('resources.limits.memory.app_init') }}
      app_migrate: {{ @('resources.limits.memory.app_migrate') }}
      elasticsearch: {{ @('resources.limits.memory.elasticsearch') }}
      mysql: {{ @('resources.limits.memory.mysql') }}
      php_fpm_exporter: {{ @('resources.limits.memory.php_fpm_exporter') }}
      postgres: {{ @('resources.limits.memory.postgres') }}
      redis: {{ @('resources.limits.memory.redis') }}
      redis_session: {{ @('resources.limits.memory.redis_session') }}
      jenkins_runner: {{ @('resources.limits.memory.jenkins_runner') }}
      jenkins: {{ @('resources.limits.memory.jenkins') }}
      rabbitmq: {{ @('resources.limits.memory.rabbitmq') }}
  limits:
    memory:
      console: {{ @('resources.limits.memory.console') }}
      cron: {{ @('resources.requests.memory.cron') }}
      nginx: {{ @('resources.limits.memory.nginx') }}
      php_fpm: {{ @('resources.limits.memory.php_fpm') }}
      app_init: {{ @('resources.limits.memory.app_init') }}
      app_migrate: {{ @('resources.limits.memory.app_migrate') }}
      elasticsearch: {{ @('resources.limits.memory.elasticsearch') }}
      mysql: {{ @('resources.limits.memory.mysql') }}
      php_fpm_exporter: {{ @('resources.limits.memory.php_fpm_exporter') }}
      postgres: {{ @('resources.limits.memory.postgres') }}
      redis: {{ @('resources.limits.memory.redis') }}
      redis_session: {{ @('resources.limits.memory.redis_session') }}
      jenkins_runner: {{ @('resources.limits.memory.jenkins_runner') }}
      jenkins: {{ @('resources.limits.memory.jenkins') }}
      rabbitmq: {{ @('resources.limits.memory.rabbitmq') }}
