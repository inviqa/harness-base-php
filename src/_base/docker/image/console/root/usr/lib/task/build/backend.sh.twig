#!/bin/bash

function task_build_backend()
{(
    set -e -o pipefail

    cd {{ @('backend.path') }}

    if {{ @('backend.build.when')|raw }}; then
        :
    else
        return 0;
    fi

    {% for step in @('backend.build.steps') -%}
    {{ step|raw }}
    {% endfor %}

)}
