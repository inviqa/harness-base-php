#!/bin/bash

function task_build_frontend()
{(
    set -e -o pipefail

    cd {{ @('frontend.path') }}

    if {{ @('frontend.build.when') }}; then
        :
    else
        return 0;
    fi

    {% for step in @('frontend.build.steps') -%}
    {{ step }}
    {% endfor %}

)}
