#!/bin/bash

function task_build_frontend()
{(
    set -e -o pipefail

    cd {{ @('frontend.path') }}

    if {{ @('frontend.build.when')|raw }}; then
        :
    else
        return 0;
    fi

    {% for step in @('frontend.build.steps') -%}
    {{ step|raw }}
    {% endfor %}

)}
