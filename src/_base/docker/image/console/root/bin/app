#!/bin/bash
set -o errexit
set -o pipefail
set -o nounset

main()
{
    task "$@"
}

bootstrap()
{
    export NVM_DIR="$HOME/.nvm"
    # shellcheck source=/home/build/nvm.sh
    [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
    . /lib/sidekick.sh
}

bootstrap
main "$@"
