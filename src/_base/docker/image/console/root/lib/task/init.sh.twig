#!/bin/bash
set -o errexit
set -o pipefail
set -o nounset

function task_init()
{
    task "database:available"
    task "assets:apply"

    {% for step in @('backend.init.steps') -%}
    {{ step|raw }}
    {% endfor %}

    task "welcome"
}
