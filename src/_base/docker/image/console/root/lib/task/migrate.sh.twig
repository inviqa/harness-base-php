#!/bin/bash

function task_migrate()
{
    task "database:available"

    if ! db_hasSchema; then
        task "init"
    fi

    {% for step in @('backend.migrate.steps') -%}
    {{ step|raw }}
    {% endfor %}
}
