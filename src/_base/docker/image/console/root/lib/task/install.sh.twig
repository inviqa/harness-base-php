#!/bin/bash
set -o errexit
set -o pipefail
set -o nounset

function task_install()
{
    task "database:available"

    {% for step in @('backend.install.steps') -%}
    {{ step|raw }}
    {% endfor %}

}
