  solr:
{% if @('services.solr.major_version') == 4 %}
    build:
      context: .my127ws/docker/image/solr4/
      args:
        SOLR_CORE_NAME: collection1
{% else %}
    image: {{ @('services.solr.image') }}
{% endif %}
    environment:
      SOLR_CORE_NAME: collection1
    labels:
      - traefik.backend=solr-{{ @('workspace.name') }}
      - traefik.frontend.rule=Host:solr-{{ @('hostname') }}
      - traefik.docker.network=my127ws
      - traefik.port=8983
    command:
      - solr-precreate
      - collection1
      - /opt/solr/server/solr/configsets/collection1
    volumes:
{% if @('services.solr.major_version') == 4 %}
      - .my127ws/docker/image/solr4/root/opt/solr/server/solr/configsets/:/opt/solr/server/solr/configsets/
{% else %}
      - .my127ws/docker/image/solr/root/opt/solr/server/solr/configsets/:/opt/solr/server/solr/configsets/
{% endif %}
      - solr_data:/usr/local/share/solr/collection1/data/
    networks:
      - private
      - shared
