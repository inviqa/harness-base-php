{%- set blocks  = '_twig/docker-compose.yml/' %}
{%- set configs = [
  from_yaml(include(blocks ~ 'service/nginx.yml.twig')),
  from_yaml(include(blocks ~ 'service/php-fpm.yml.twig')),
] %}

{{- to_nice_yaml(deep_merge(configs), 2, 0) | trim }}